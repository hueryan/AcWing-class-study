# 查看命令

```shell
ls --help 
或者 
man ls
```

```shell
ls -l | wc -l
```

## 常用命令

```
(1) ctrl c: 取消命令，并且换行
(2) ctrl u: 清空本行命令
() ↑：切换上一个命令 
(3) tab键：可以补全命令和文件名，如果补全不了快速按两下tab键，可以显示备选选项
(4) ls: 列出当前目录下所有文件，蓝色的是文件夹，白色的是普通文件，绿色的是可执行文件 
ls -l 查看详情
ls ~/hujing -lh 人性化输出 如大小单位kb,mb
ls -a 隐藏文件
ls -A 不显示 . ..
ll 等价于 ls -la
(5) pwd: 显示当前路径
(6) cd XXX: 进入XXX目录下
cd .. 返回上层目录 
cd - 返回切换前目录 
cd ~或者cd 回到家目录
(7) mkdir XXX: 创建目录XXX
mkdir y\ c 创建一个 `y c` 文件夹 
删除的时候 rm y\ c
mkdir -p a/b/c 
(8) rm XXX: 删除普通文件;  
rm -r XXX : 递归删除文件夹
rm a/* 删除a下所有
rm *.txt
(9) tree 查看当前目录结构
(10) cp XXX YYY: 将XXX文件复制成YYY，XXX和YYY可以是一个路径，比如../dir_c/a.txt，表示上层目录下的dir_c文件夹下的文件a.txt
cp a b -r 直接将a目录复制到b目录 包括a
cp a c -r 将a复制到当前目录名且重命名为c
cp a/tmp.txt b/ 复制到b
cp a/tmp.txt b/tmp2.txt 同时也更名了
(11) mv XXX YYY: 将XXX文件移动到YYY，和cp命令一样，XXX和YYY可以是一个路径；重命名也是用这个命令
(12) touch XXX: 创建一个文件
(13) cat XXX: 展示文件XXX中的内容
(14) 复制文本
    windows/Linux下：Ctrl + insert，Mac下：command + c
(15) 粘贴文本
    windows/Linux下：Shift + insert，Mac下：command + v
(16) top 查看运行进程 （q 退出）
(17) ps aux | grep vim 查看vim进程
(18) kill -s 9 PID 杀死PID进程
```

## 作业

```bash
创建好作业后，先进入文件夹/home/acs/homework/lesson_1/，然后：
(0) 进入homework_0文件夹，分别创建文件夹dir_a, dir_b, dir_c
<---
mkdir dir_a dir_b dir_c
--->

(1) 进入homework_1文件夹，将a.txt, b.txt, c.txt 分别复制成: a.txt.bak, b.txt.bak, c.txt.bak
<---
cp a.txt a.txt.bak
cp b.txt b.txt.bak
cp c.txt c.txt.bak
--->

<---shell
cd ~/homework/lesson_1/homework_1/
for var in *.txt;
do
    cp "$var" "$var.bak"
done
--->
(2) 进入homework_2文件夹，将a.txt, b.txt, c.txt 分别重命名为: a_new.txt, b_new.txt, c_new.txt
<---
mv a.txt a_new.txt
mv b.txt b_new.txt
mv c.txt c_new.txt
--->
(3) 进入homework_3文件夹，将dir_a文件夹下的a.txt, b.txt, c.txt分别移动到文件夹dir_b下
<---
mv dir_a/* dir_b
--->

<---shell
cd ~/homework/lesson_1/homework_3
mv dir_a/{a..c}.txt dir_b
--->
(4) 进入homework_4文件夹，将普通文件a.txt, b.txt, c.txt删除
rm *.txt
<---
rm -r *.txt
--->
(5) 进入homework_5文件夹，将文件夹dir_a, dir_b, dir_c删除
<---
rm -rf * 
--->
(6) 进入homework_6文件夹，查看task.txt的内容，并按其指示进行操作
<---
cat task.txt
mkdir dir_a
mv task.txt dir_a/done.txt
--->
(7) 进入homework_7文件夹，创建文件夹dir_0, dir_1, dir_2，
    将a.txt, b.txt, c.txt复制到dir_0下，重命名为a0.txt, b0.txt, c0.txt;
    将a.txt, b.txt, c.txt复制到dir_1下，重命名为a1.txt, b1.txt, c1.txt;
    将a.txt, b.txt, c.txt复制到dir_2下，重命名为a2.txt, b2.txt, c2.txt;
<---
mkdir dir_0 dir_1 dir_2 
cp a.txt dir_0/a0.txt
cp b.txt dir_0/b0.txt
cp c.txt dir_0/c0.txt
cp a.txt dir_1/a1.txt
cp b.txt dir_1/b1.txt
cp c.txt dir_1/c1.txt
cp a.txt dir_2/a2.txt
cp b.txt dir_2/b2.txt
cp c.txt dir_2/c2.txt
--->

<---shell
cd ~/homework/lesson_1/homework_7
for i in {0..2}
do
    mkdir "dir_$i"
    for var in *.txt
    do
        cp "$var" "dir_$i/${var%.txt}$i.txt"
    done
done
--->

<---命令行
cd ~/homework/lesson_1/homework_7
for i in {0..2}; do (mkdir "dir_$i"; for var in *.txt; do cp "$var" "dir_$i/${var%.txt}$i.txt"; done;); done;
--->
(8) 进入homework_8文件夹，分别在dir_a, dir_b, dir_c文件夹下查看task.txt的内容，并分别按照指示进行操作
<---
cat dir_a/task.txt
rm dir_a/a.txt
cat dir_b/task.txt
mv dir_b/b.txt dir_b/b_new.txt
cat dir_c/task.txt
cp dir_c/c.txt dir_c/c.txt.bak
--->
(9) 进入homework_9文件夹，将其中所有txt类型的文件删除
<---
rm *.txt
--->
```



